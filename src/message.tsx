
// PascalCasing in react our component namw should be start from capital letter

const Message = ()=>{
    const name="vikas"
 return(
    <div>
        <h1>hello {name} !</h1>
       </div>
 )
}

export default Message;   // export is neccessary
// a component always returns a single element 